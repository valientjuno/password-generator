let generatePass = [];
const text = document.getElementById("text");
const 

// get length
let length = parseInt(document.querySelector("length").value);
document.getElementById("length").text;

// random index sets
let randomIndex = Math.floor(Math.random() * charSet.length);